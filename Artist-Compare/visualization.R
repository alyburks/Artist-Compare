##Visualization. Alyxis will do this once we talk about it.

#require(plotly)

#visualization1 <- function(data){ #data should be a list of three lists. only need the first two
#  plot_ly(
#    x = c(test$name_artist, test2$name_artist),
#    y = c(test$pop_artist, test2$pop_artist),
#    type = "bar",
#    marker = list(color = toRGB("gray30"))
#  ) %>% 
#    layout(xaxis = list(title = ""), 
#           yaxis = list(title = ""),
#           paper_bgcolor = toRGB("gray50"),
#           plot_bgcolor = toRGB("gray50"))
#}

